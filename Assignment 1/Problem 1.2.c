#include <stdio.h>

/*
    JTSK-320111
    a1_p2.c
    Fezile Manana
    f.manana@jacobs-university.de
*/

int main() {
    int result; /* The result of our calculation */
    result = (2+7) * 9 / 3;
    printf("The result is %d\n", result); /* the programme was printing a random variable "d" until we specify "result to be displayed */

    getchar();
    return 0;
}
